<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mesad.mes.mapper.MesReportMapper">

    <resultMap type="MesReport" id="MesReportResult">
        <result property="reportId"       column="report_id"       />
        <result property="reportName"     column="report_name"     />
        <result property="reportType"     column="report_type"     />
        <result property="reportContent"  column="report_content"  />
        <result property="status"         column="status"          />
        <result property="remark"         column="remark"          />
        <result property="createBy"       column="create_by"       />
        <result property="createTime"     column="create_time"     />
        <result property="updateBy"       column="update_by"       />
        <result property="updateTime"     column="update_time"     />
    </resultMap>

    <sql id="selectMesReportVo">
        select report_id, report_name, report_type, report_content, status, remark, create_by, create_time, update_by, update_time from mes_report
    </sql>

    <select id="selectMesReportList" parameterType="MesReport" resultMap="MesReportResult">
        <include refid="selectMesReportVo"/>
        <where>
            <if test="reportName != null and reportName != ''"> and report_name like concat('%', #{reportName}, '%')</if>
            <if test="reportType != null and reportType != ''"> and report_type = #{reportType}</if>
            <if test="status != null and status != ''"> and status = #{status}</if>
        </where>
    </select>

    <select id="selectMesReportByReportId" parameterType="Long" resultMap="MesReportResult">
        <include refid="selectMesReportVo"/>
        where report_id = #{reportId}
    </select>

    <insert id="insertMesReport" parameterType="MesReport" useGeneratedKeys="true" keyProperty="reportId">
        insert into mes_report
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="reportName != null and reportName != ''">report_name,</if>
            <if test="reportType != null and reportType != ''">report_type,</if>
            <if test="reportContent != null">report_content,</if>
            <if test="status != null">status,</if>
            <if test="remark != null">remark,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="reportName != null and reportName != ''">#{reportName},</if>
            <if test="reportType != null and reportType != ''">#{reportType},</if>
            <if test="reportContent != null">#{reportContent},</if>
            <if test="status != null">#{status},</if>
            <if test="remark != null">#{remark},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateMesReport" parameterType="MesReport">
        update mes_report
        <trim prefix="SET" suffixOverrides=",">
            <if test="reportName != null and reportName != ''">report_name = #{reportName},</if>
            <if test="reportType != null and reportType != ''">report_type = #{reportType},</if>
            <if test="reportContent != null">report_content = #{reportContent},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where report_id = #{reportId}
    </update>

    <delete id="deleteMesReportByReportId" parameterType="Long">
        delete from mes_report where report_id = #{reportId}
    </delete>

    <delete id="deleteMesReportByReportIds" parameterType="String">
        delete from mes_report where report_id in
        <foreach item="reportId" collection="array" open="(" separator="," close=")">
            #{reportId}
        </foreach>
    </delete>
</mapper>
